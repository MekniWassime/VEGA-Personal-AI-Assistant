/// VEGA core — owns the rendered UI.
///
/// The `core` package is responsible for the widget tree: theming, the root
/// application widget, and the screens. App-level concerns such as async
/// initialization live in the host app (see `vega_app`).
library;

export 'src/vega_app.dart';
export 'src/placeholder_screen.dart';
